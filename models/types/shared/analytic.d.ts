// Analytic types

// Event tracking token
export type TEventTrackingPayload = {
// later
};

// Pageviews token
export type TPageviewsPayload = {
// later
};

// Visitor tracking
export type TVisitorTrackingPayload = {
// later
};